# LakeMart 湖仓一体电商数据平台 — 项目快照 3.0（当前状态 + 演进历程）

> 此文档可作为 Prompt 发送给任何 AI 助手，帮助其快速理解项目的**当前状态**、**架构演进**以及**已解决的问题**。以下内容请**完整复制**，在需要时一次性发送。

---

## 一、项目概述

**LakeMart** 是一个 B2C 电商系统，集成了**湖仓一体（Lakehouse）**实时数据分析平台。项目以 Spring Boot 3 + Vue 3 构建前后端电商功能（用户端 + 管理端），以 **MinIO + Iceberg + Spark + Kafka** 实现用户行为数据的实时采集、存储与预聚合分析，并在此基础上扩展了**用户画像系统**和**个性化推荐系统**。

**GitHub 地址**：https://github.com/sunkuizhi/LakeMart


## 二、核心功能全览（当前状态）

### 电商业务层
| 端 | 功能 | 状态 |
|----|------|------|
| 用户端 | 注册/登录、商品浏览/搜索/分类筛选、购物车、下单、订单管理、收货地址管理、积分明细、个人中心 | ✅ 稳定 |
| 管理端 | 商品管理（CRUD/上下架）、订单管理（列表/状态变更）、用户管理、分类管理、轮播图管理、仪表盘 | ✅ 稳定 |

### 数据采集与实时处理
| 功能 | 说明 | 状态 |
|------|------|------|
| 用户行为埋点 | `POST /api/behavior/track`，前端在商品详情、加购、下单后调用 | ✅ 稳定 |
| Kafka 消息队列 | `user-behaviors` 主题，存储行为日志 | ✅ 稳定 |
| Spark Streaming 实时入湖 | 消费 Kafka → 写入 Iceberg（MinIO） + Redis 热榜 + MySQL 行为日志 | ✅ 稳定 |

### 离线批处理（定时调度）
| 作业 | 功能 | 输出目标 | 调度频率 |
|------|------|----------|----------|
| `HotProductsBatch` | 统计最近 1 小时热销商品 | `hot_products` (MySQL) | 每 5 分钟 |
| `BehaviorTrendBatch` | 按分钟聚合行为趋势 | `behavior_trend` (MySQL) | 每 10 分钟 |
| `ActionDistributionBatch` | 按行为类型聚合分布 | `action_distribution` (MySQL) | 每 10 分钟 |
| `UserProfileBatch` | 计算用户画像标签 | `user_profile` (MySQL) | 每天凌晨 2 点 |

### 用户画像系统
- 生命周期阶段（新用户/成长/成熟/沉睡/流失）
- 用户等级（钻石/黄金/白银/青铜/新用户）
- 消费能力指标（总订单数、总消费金额、客单价）
- 行为指标（近 7 天/30 天行为次数、活跃天数）
- 管理端可视化页面（分层占比饼图 + 等级分布柱状图 + 用户列表）

### 推荐系统
- `GET /api/user/recommend` 推荐接口
- **三级推荐策略**：偏好品类推荐 → 相似商品推荐（协同过滤） → 热销商品兜底
- **推荐理由展示**：每个推荐商品附带推荐理由（如“基于你的偏好品类：手机通讯”）
- **Redis 缓存**：推荐结果缓存 1 小时，提升性能


## 三、项目结构（当前状态）

```
LakeMart/
├── docker-compose.yml
├── pom.xml
│
├── lakemart-server/                 # Spring Boot 后端
│   └── src/main/java/org/lzx/lakemart/
│       ├── controller/
│       │   ├── admin/                # 管理端 Controller（6 个）
│       │   └── client/               # 用户端 Controller（8 个）
│       ├── service/
│       │   ├── admin/                # 管理端 Service
│       │   ├── client/               # 用户端 Service
│       │   └── common/               # 公共 Service
│       ├── task/
│       │   ├── DataSimulator.java    # 数据模拟器（可按需关闭）
│       │   ├── SparkBatchScheduler.java  # Spark 批处理定时调度
│       │   └── TempFileCleanupTask.java
│       └── model/vo/
│           └── RecommendProductVO.java   # 推荐商品 VO（含推荐理由）
│
├── lakemart-admin/                  # 管理端前端（Vue 3）
│   └── src/views/
│       ├── user/UserProfile.vue      # 用户画像页面
│       ├── dashboard/Dashboard.vue
│       └── ...
│
├── lakemart-client/                 # 用户端前端（Vue 3）
│   └── src/views/HomeView.vue        # 首页（含推荐商品区块）
│
└── lakemart-spark/                  # Spark 大数据模块
    ├── streaming/
    │   └── RealtimeBehaviorStreaming.scala   # 实时入湖
    └── batch/
        ├── HotProductsBatch.scala            # 热销商品
        ├── BehaviorTrendBatch.scala          # 行为趋势
        ├── ActionDistributionBatch.scala     # 行为分布
        └── UserProfileBatch.scala            # 用户画像
```


## 四、架构演进历程

### 阶段一：传统电商 + 日志采集（1.0 快照）
- ✅ 完整的电商前后端
- ✅ 用户行为日志采集链路（埋点 → Kafka）
- ❌ 大数据分析能力缺失

### 阶段二：湖仓一体建设（2.0 快照）
- ✅ Spark Streaming 实时数据入湖（Iceberg）
- ✅ 三个离线批处理作业（热销/趋势/分布）
- ✅ 管理端仪表盘可视化
- ⚠️ 批处理调度尚未自动化
- ⚠️ 实时与离线数据口径不一致

### 阶段三：系统完善与重构（当前 3.0）
| 优化项 | 说明 | 状态 |
|--------|------|------|
| **批处理定时调度** | `SparkBatchScheduler` 集成 Spring `@Scheduled`，自动执行四个 Spark 批处理作业 | ✅ |
| **项目结构重构** | Controller 和 Service 按 `admin/client/common` 拆分，删除冗余空文件 | ✅ |
| **用户画像系统** | 新增 `UserProfileBatch` 作业 + 管理端可视化页面 | ✅ |
| **个性化推荐系统** | 三级推荐策略（偏好品类 → 相似商品 → 热销兜底）+ 推荐理由展示 | ✅ |
| **推荐结果缓存** | Redis 缓存推荐结果，提升接口响应速度 | ✅ |
| **热销兜底数据** | 修复 `hot_products` 表为空导致推荐失败的问题 | ✅ |
| **SQL 兼容性修复** | 修复 `DISTINCT + ORDER BY` 在 MySQL 中的兼容性问题 | ✅ |
| **日志级别优化** | 减少控制台刷屏，提升调试效率 | ✅ |


## 五、已解决的关键问题

| 问题 | 根因 | 解决方案 |
|------|------|----------|
| Spark 作业执行失败 | JDK 版本不兼容（Spark 需要 JDK 17） | Spark 模块单独使用 JDK 17，后端继续用 JDK 21 |
| 批处理作业无法自动运行 | 缺少定时调度机制 | `SparkBatchScheduler` 集成 Spring `@Scheduled` |
| 推荐接口返回“系统繁忙” | `hot_products` 表为空导致热销兜底失败 | 手动插入热销数据，并修复 fallback SQL |
| 推荐商品名称不显示 | `DISTINCT + ORDER BY` SQL 在 MySQL 中不兼容 | 改用 `GROUP BY + MAX(create_time)` |
| 商品无法点击进入详情 | `productId` 字段为 `undefined` | 修复前端字段映射 + `@RequestParam(name = "...")` |
| Kafka 连接不上 | Docker 容器未启动 | `docker-compose up -d kafka zookeeper` |
| 模拟器刷屏 | `DataSimulator` 日志级别过高 | 删除 `spring.profiles.active: dev` 关闭模拟器，或调高日志级别 |
| `@RequestParam` 参数绑定失败 | Spring Boot 3 要求显式指定 `name` | 所有 `@RequestParam` 统一添加 `name` 属性 |


## 六、技术栈总览

| 层级 | 技术 | 版本 |
|------|------|------|
| 后端框架 | Spring Boot | 3.5.13 |
| 后端语言 | Java | 21 |
| ORM | MyBatis-Plus | 3.5.13 |
| 业务数据库 | MySQL | 8.0 |
| 缓存 | Redis | 7 |
| 对象存储 | MinIO | 最新 |
| 消息队列 | Kafka | 3.7.0 |
| 协调服务 | Zookeeper | 3.7 |
| 大数据计算 | Apache Spark | 3.5.0（JDK 17） |
| 数据湖表格式 | Apache Iceberg | 1.6.1 |
| 前端框架 | Vue 3 + Vite | 3.5 / 8.0 |
| UI 组件库 | Element Plus | 2.13.7 |
| 图表 | ECharts | 6.0.0 |
| 状态管理 | Pinia | — |
| 部署 | Docker / Docker Compose | — |


## 七、环境与配置

### Docker 服务端口
| 服务 | 宿主机端口 | 容器端口 |
|------|------------|----------|
| MySQL | 3307 | 3306 |
| Redis | 6379 | 6379 |
| MinIO | 9000 / 9001 | 9000 / 9001 |
| Zookeeper | 2181 | 2181 |
| Kafka | 9092 | 9092 |
| Backend | 8080 | 8080 |
| Admin Frontend | 5173 | 80 |
| Client Frontend | 5174 | 80 |

### JDK 版本
| 模块 | JDK 版本 |
|------|----------|
| `lakemart-server` | JDK 21 |
| `lakemart-spark` | JDK 17（**必须**） |

### Spark 作业 VM Options
```
--add-opens java.base/sun.nio.ch=ALL-UNNAMED
--add-opens java.base/java.nio=ALL-UNNAMED
--add-opens java.base/java.lang=ALL-UNNAMED
--add-opens java.base/jdk.internal.ref=ALL-UNNAMED
-Dio.netty.tryReflectionSetAccessible=true
-Dspark.ui.enabled=false
```


## 八、常用调试命令

```bash
# 启动所有基础设施
docker-compose up -d

# 查看 Kafka 消息
docker exec -it lakemart-kafka /opt/kafka/bin/kafka-console-consumer.sh \
  --bootstrap-server localhost:9092 --topic user-behaviors --from-beginning

# 查看 Redis 热榜
docker exec -it lakemart-redis redis-cli ZREVRANGE hot:products 0 9 WITHSCORES

# 清空 Redis 缓存
docker exec -it lakemart-redis redis-cli FLUSHDB

# 查看推荐缓存
docker exec -it lakemart-redis redis-cli KEYS "recommend:*"
```


## 九、下一步待办

| 优先级 | 任务 | 说明 |
|--------|------|------|
| **高** | 部署上线 | 打包 Docker 镜像，部署到云服务器 |
| **中** | 推荐算法优化 | 引入实时行为权重、时间衰减 |
| **中** | 推荐埋点反馈 | 记录用户对推荐商品的点击/购买 |
| **低** | 实时告警系统 | 异常行为监控 + WebSocket 推送 |
| **低** | Iceberg 小文件合并 | 优化数据湖存储性能 |


## 十、注意事项

- **JDK 版本**：后端用 JDK 21，Spark 模块必须用 JDK 17
- **Kafka 连接**：本地用 `localhost:9092`，Docker 内用 `kafka:9092`
- **推荐缓存**：修改推荐逻辑后需清空 Redis 缓存 `FLUSHDB`
- **所有 `@RequestParam` 必须显式指定 `name` 属性**
- **模拟器**：可通过删除 `spring.profiles.active: dev` 关闭

---

此快照可用于后续对话中快速让 AI 理解项目的**全貌**、**演进历程**和**已解决的问题**。如需进一步细化某一部分，可在此基础上补充。