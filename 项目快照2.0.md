好的，我为你整理了一份**项目快照 Prompt**，你可以将其保存下来，用于后续与任何 AI 交流时快速同步上下文。这份描述涵盖了项目的技术选型、架构、模块、数据流、当前状态和待办事项，足够详细且易于理解。

---

# LakeMart 项目快照（用于 AI 上下文同步）

## 1. 项目概述
**LakeMart** 是一个 B2C 电商系统，集成了**湖仓一体（Lakehouse）** 实时数据分析平台。项目以 Spring Boot 3 + Vue 3 构建前端电商功能（用户端 + 管理端），并以 **MinIO + Iceberg + Spark + Kafka** 实现用户行为数据的实时采集、存储与预聚合分析，最终在管理端仪表盘展示可视化图表（订单趋势、销售额、热销商品、行为趋势、行为分布）。

**GitHub 地址**：https://github.com/sunkuizhi/LakeMart

---

## 2. 技术栈

### 后端
- Spring Boot 3.5.13 + JDK 21
- Spring Security + JWT（基于邮箱登录，角色区分）
- MyBatis-Plus 3.5.13 + MySQL 8
- Redis 7（验证码、缓存）
- Spring Kafka（生产者）
- MinIO 8.5.17（图片/文件存储）
- Spring Mail（邮件验证码）

### 前端
- Vue 3 + Vite + TypeScript
- Element Plus（UI 组件）
- ECharts（图表）
- Axios + Pinia + Vue Router

### 大数据
- Apache Kafka 3.7.0 + Zookeeper（消息队列）
- Apache Spark 3.5.0（流式 + 批处理），运行于 JDK 17
- Apache Iceberg 1.6.1（数据湖表格式，存储于 MinIO）
- MinIO（数据湖底层存储，兼容 S3）
- MySQL 8（预聚合结果表，供管理端查询）

---

## 3. 系统架构（数据流）

```
用户端/管理端埋点 → 后端 /api/behavior/track → Kafka (topic: user-behaviors)
                                                   ↓
                                            Spark Streaming (RealtimeBehaviorStreaming)
                                                   ↓
                                           Iceberg 表 (MinIO, lake.default.user_behaviors)
                                                   ↓
                                            Spark 批处理 (定时)
                                               ├─ HotProductsBatch → hot_products (MySQL)
                                               ├─ BehaviorTrendBatch → behavior_trend (MySQL)
                                               └─ ActionDistributionBatch → action_distribution (MySQL)
                                                   ↓
                                          管理端 REST API (/api/admin/statistics/*)
                                                   ↓
                                          管理端前端 ECharts 仪表盘
```

---

## 4. 项目目录结构

```
LakeMart/
├── docker-compose.yml               # 一键启动所有服务（MySQL, Redis, Kafka, MinIO, Spark 等）
├── lakemart-server/                 # Spring Boot 后端
│   ├── src/main/java/org/lzx/lakemart/
│   │   ├── controller/              # 控制器（含 BehaviorController, AdminStatisticsController 等）
│   │   ├── service/                 # 服务接口与实现（含 StatisticsService）
│   │   ├── mapper/                  # MyBatis-Plus Mapper（含 StatisticsMapper）
│   │   ├── model/                   # entity, dto, vo
│   │   ├── kafka/                   # Kafka 生产者（BehaviorProducer）
│   │   ├── config/                  # 配置类（Security, Minio, MyBatis-Plus 等）
│   │   └── filter/                  # JWT 过滤器
│   └── src/main/resources/
│       ├── application.yml
│       └── mapper/                  # XML 映射文件（含 StatisticsMapper.xml）
├── lakemart-admin/                  # 管理端 Vue 3 项目
├── lakemart-client/                 # 用户端 Vue 3 项目
├── lakemart-spark/                  # Spark 大数据模块（Scala + Java）
│   ├── src/main/scala/org/lzx/lakemart/spark/
│   │   ├── streaming/               # RealtimeBehaviorStreaming.scala
│   │   └── batch/                   # HotProductsBatch, BehaviorTrendBatch, ActionDistributionBatch
│   └── pom.xml                      # 包含 Spark, Iceberg, Hadoop-AWS 依赖
└── sql/                             # 数据库建表脚本（可选）
```

---

## 5. 核心功能模块

### 电商业务（已完成）
- **用户端**：注册登录、商品列表/详情、购物车、下单、订单管理、个人中心、积分明细。
- **管理端**：商品管理（CRUD、上下架）、订单管理（列表、状态变更、详情）、用户管理、分类管理、轮播图管理。

### 用户行为采集（已完成）
- 后端埋点接口：`POST /api/behavior/track`（需登录，JWT 认证）。
- 字段：`action`（VIEW_PRODUCT, ADD_CART, BUY 等）、`productId`、`ts`（ISO 时间字符串）。
- 前端已在用户端商品详情、加购、下单后调用此接口。

### 实时数据入湖（已完成）
- Spark Streaming 作业 `RealtimeBehaviorStreaming` 消费 Kafka `user-behaviors`，解析 JSON 并写入 Iceberg 表 `lake.default.user_behaviors`。
- 表 schema：`user_id BIGINT, action STRING, product_id BIGINT, event_time TIMESTAMP`。
- 存储格式：Parquet，按 `event_time` 隐藏分区（Iceberg 自动管理）。
- 作业运行于 JDK 17，禁用 Web UI (`spark.ui.enabled=false`)。

### 批处理预聚合（已完成）
三个独立 Spark 批处理作业，运行于 JDK 17，可定期调度：
1. **HotProductsBatch**：从 Iceberg 表统计最近 1 小时热销商品，写入 MySQL `hot_products`（product_id, cnt）。
2. **BehaviorTrendBatch**：按分钟聚合最近 60 分钟行为总次数，写入 MySQL `behavior_trend`（minute, cnt）。
3. **ActionDistributionBatch**：按 action 聚合总次数，写入 MySQL `action_distribution`（action, cnt）。

### 管理端数据可视化（已完成）
- 仪表盘包含 5 个 ECharts 图表：
  - 近七日订单趋势（从 MySQL `tb_order` 统计）
  - 近 7 日销售额趋势（从 MySQL `tb_order` 统计）
  - 热销商品 TOP 10（从 `hot_products` 查询）
  - 实时行为趋势（从 `behavior_trend` 查询）
  - 用户行为分布（从 `action_distribution` 查询）
- 概览卡片：总订单数、总销售额、总用户数、热销商品数（由 `/overview` 接口提供）。

---

## 6. 环境与配置

### Docker 服务（docker-compose.yml）
- MySQL 8：端口 3307（宿主机），数据库 `lakemart`
- Redis 7：端口 6379
- Zookeeper：端口 2181
- Kafka：端口 9092（`KAFKA_ADVERTISED_HOST_NAME=localhost`）
- MinIO：端口 9000/9001（控制台），桶名 `lakemart-warehouse`（数据湖）
- Spark 容器（可选，实际在 IDEA 中运行）

### 后端配置（application.yml）
- 数据库：`jdbc:mysql://localhost:3307/lakemart`
- Redis：`localhost:6379`
- Kafka：`localhost:9092`
- MinIO：`http://localhost:9000`
- JWT 密钥、邮件配置等

### Spark 运行参数（IDEA 运行配置）
- JDK 17
- VM options:
  ```
  --add-opens java.base/sun.nio.ch=ALL-UNNAMED
  --add-opens java.base/java.nio=ALL-UNNAMED
  --add-opens java.base/java.lang=ALL-UNNAMED
  --add-opens java.base/jdk.internal.ref=ALL-UNNAMED
  -Dio.netty.tryReflectionSetAccessible=true
  ```

---

## 7. 已解决的关键问题与注意事项

- **Spark 与 JDK 21 不兼容**：大数据模块统一使用 JDK 17，后端仍用 JDK 21。
- **Servlet 冲突导致 Spark 启动失败**：添加 `spark.ui.enabled=false`。
- **Kafka 主题分区变化导致 checkpoint 失效**：删除 checkpoint 目录（`data/spark/checkpoints`），重建主题（分区数固定为 1）。
- **JSON 字段名不匹配**：后端序列化使用驼峰（userId, productId），Spark 解析路径需对应修改为 `$.userId`、`$.productId`。
- **MyBatis-Plus 插入缺少字段**：如 `tb_address.user_id`，已在 Mapper XML 中手工编写 INSERT 语句。
- **Trino 放弃**：因配置复杂且与 Iceberg 版本兼容性问题，改为 Spark 批处理预聚合 + MySQL 缓存。
- **管理端权限**：所有 `/api/admin/**` 接口需 `ROLE_ADMIN`，可通过数据库修改用户角色。
- **批处理表**：需在 MySQL 中预先创建 `hot_products`、`behavior_trend`、`action_distribution` 表（已有建表脚本）。

---

## 8. 当前状态

- ✅ 电商前后端全功能正常。
- ✅ 埋点、Kafka、Spark Streaming 实时入湖正常（已验证，MinIO 中可见 Parquet 文件）。
- ✅ 三个批处理作业可运行（exit code 0），但预聚合表可能为空（因 Iceberg 中数据不足，或时间范围限制）。
- ✅ 管理端所有接口正常，图表组件已对接，若表中有数据则自动显示。
- ✅ 项目可在本地 IDE 中完整运行（Docker 容器 + 后端 + 前端 + Spark 作业）。

---

## 9. 待办与扩展方向

- **定时调度**：将批处理作业加入定时任务（如每 5 分钟）实现自动更新。
- **真实数据采集**：在用户端更多页面（如商品列表、搜索）集成埋点，产生真实行为数据。
- **性能优化**：Iceberg 小文件合并，Spark 参数调优。
- **更多分析指标**：UV、PV、用户留存等。
- **部署文档**：编写完整 README 和部署指南。

---

## 10. 开发环境要求

- JDK 21（后端）+ JDK 17（Spark 作业）
- Docker Desktop（运行依赖服务）
- IDEA 2023.2+（Scala 插件需安装）
- Node.js 18+（前端）
- Maven 3.8+

---

此 Prompt 可用于后续对话中快速让 AI 理解项目全貌，以便获得更精准的技术支持。如需进一步细化某一部分，可在此基础上补充。