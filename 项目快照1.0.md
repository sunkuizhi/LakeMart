# LakeMart 湖仓一体电商数据平台 — 项目快照（Prompt）

> 你可以将此文档作为 Prompt 发送给任何 AI 助手，让其快速理解项目的完整上下文，以便继续开发、调试或重构。以下内容请**完整复制**，在需要时一次性发送。

---

## 项目概述

LakeMart 是一个**湖仓一体架构**的电商数据平台，融合了**完整的电商业务闭环**与**实时/离线大数据分析能力**。项目定位为个人技术实践项目，目标是构建一个具备商品管理、订单交易、用户行为日志采集、实时热门商品统计、离线用户画像生成等功能的综合性数据平台。项目采用前后端分离架构，后端基于 Spring Boot 3，前端使用 Vue 3，大数据部分基于 Spark 和 Kafka，所有服务均通过 Docker Compose 一键部署，已开源在 GitHub。

---

## 一、技术栈总览

### 后端（Spring Boot 3）
| 组件 | 版本 | 用途 |
|------|------|------|
| Spring Boot | 3.5.13 | 主框架 |
| JDK | 21 | 运行环境 |
| MyBatis-Plus | 3.5.13 | ORM 框架 |
| MySQL | 8.0 | 业务数据库（容器） |
| Redis | 7 | 缓存 + 实时热门商品存储（容器） |
| MinIO | 最新 | 对象存储（图片、日志）（容器） |
| Kafka | 3.5+ | 消息队列（容器） |
| Spark | 3.5.4 | 大数据计算引擎 |
| JWT | 0.12.6 | 身份认证 |
| Spring Security | 6.5.9 | 权限控制 |
| Spring Mail | — | 邮件发送（验证码） |
| Hutool | 5.8.32 | 工具库 |
| Lombok | — | 简化代码 |

### 前端（Vue 3）
| 组件 | 版本 | 用途 |
|------|------|------|
| Vue | 3.5 | 前端框架 |
| Vite | 8.0.10 | 构建工具 |
| Element Plus | 2.13.7 | UI 组件库 |
| ECharts | 6.0.0 | 数据图表 |
| Axios | — | HTTP 客户端 |
| Pinia | — | 状态管理 |
| TypeScript | — | 类型安全 |

### 部署与基础设施
| 组件 | 说明 |
|------|------|
| Docker / Docker Compose | 容器化部署，一键启动全部服务 |
| 数据卷 | mysql-data, redis-data, minio-data（持久化） |
| 网络 | 自定义桥接网络 `lakemart-net` |

---

## 二、项目架构

### 整体架构图（概念）

```
┌─────────────────────────────────────────────────────────────┐
│                      客户端层                               │
│  ┌─────────────┐    ┌─────────────────────────────────────┐ │
│  │ 用户端 (H5)  │    │         管理端 (后台)               │ │
│  │ Vue 3 + EP  │    │      Vue 3 + EP + ECharts          │ │
│  └─────────────┘    └─────────────────────────────────────┘ │
└──────────────────────────┬──────────────────────────────────┘
                           │ HTTPS / HTTP
┌──────────────────────────▼──────────────────────────────────┐
│                      接入层（可选）                          │
│                    Nginx 反向代理                           │
└──────────────────────────┬──────────────────────────────────┘
                           │
┌──────────────────────────▼──────────────────────────────────┐
│                    业务层 (Spring Boot)                     │
│  ┌─────────┐ ┌─────────┐ ┌─────────┐ ┌───────────────────┐ │
│  │ 用户认证 │ │ 商品管理 │ │ 订单管理 │ │ 购物车/地址/积分  │ │
│  └─────────┘ └─────────┘ └─────────┘ └───────────────────┘ │
│  ┌─────────┐ ┌─────────┐ ┌─────────┐ ┌───────────────────┐ │
│  │分类管理  │ │轮播图管理│ │仪表盘   │ │ 个人中心          │ │
│  └─────────┘ └─────────┘ └─────────┘ └───────────────────┘ │
│  ┌─────────────────────────────────────────────────────────┐ │
│  │         日志采集 API (/api/user/action)                 │ │
│  └─────────────────────────────────────────────────────────┘ │
└──────────────────────────┬──────────────────────────────────┘
                           │
┌──────────────────────────▼──────────────────────────────────┐
│                      数据层                                 │
│  ┌─────────┐ ┌─────────┐ ┌─────────┐ ┌───────────────────┐ │
│  │  MySQL  │ │  Redis  │ │  MinIO  │ │     Kafka         │ │
│  │(业务数据)│ │(缓存/热榜)│ │(图片/日志)│ │   (消息队列)      │ │
│  └─────────┘ └─────────┘ └─────────┘ └───────────────────┘ │
└──────────────────────────┬──────────────────────────────────┘
                           │
┌──────────────────────────▼──────────────────────────────────┐
│                   大数据计算层                              │
│  ┌──────────────────────┐    ┌────────────────────────────┐ │
│  │   Spark Streaming    │    │      Spark Batch           │ │
│  │  (实时热门商品统计)   │    │   (用户画像/日报)          │ │
│  └──────────────────────┘    └────────────────────────────┘ │
└─────────────────────────────────────────────────────────────┘
```

### 数据流路径
1. **实时流**：前端埋点 → `/api/user/action` → Kafka (`user-actions`) → Spark Streaming → Redis (ZSET)
2. **离线流**：Kafka → MinIO (Parquet) → Spark Batch → MySQL (`user_tag`, `daily_report`)

---

## 三、项目目录结构

```
LakeMart/                                  # 项目根目录（Maven 父工程）
├── docker-compose.yml                     # Docker Compose 编排文件
├── .gitignore
├── README.md                              # 项目说明文档
├── pom.xml                                # 父 POM
│
├── lakemart-server/                       # 后端 Spring Boot 模块
│   ├── Dockerfile
│   ├── pom.xml
│   ├── sql/                               # 建表脚本
│   │   └── lakemart.sql
│   └── src/main/java/org/lzx/lakemart/
│       ├── LakeMartApplication.java       # 主启动类
│       ├── config/                        # 配置类
│       │   ├── SecurityConfig.java        # Spring Security + JWT 配置
│       │   ├── MinioConfig.java
│       │   ├── MybatisPlusConfig.java
│       │   └── CorsConfig.java            # 跨域配置
│       ├── controller/                    # REST API 控制器
│       │   ├── UserController.java        # 用户认证、日志采集
│       │   ├── ProductController.java
│       │   ├── OrderController.java
│       │   ├── CartController.java
│       │   ├── AddressController.java
│       │   ├── AdminProductController.java
│       │   ├── AdminOrderController.java
│       │   ├── AdminUserController.java
│       │   ├── AdminCategoryController.java
│       │   ├── AdminBannerController.java
│       │   └── AdminStatisticsController.java
│       ├── service/                       # 业务逻辑
│       │   ├── UserService.java
│       │   ├── ProductService.java
│       │   ├── OrderService.java
│       │   └── impl/...
│       ├── mapper/                        # MyBatis-Plus Mapper
│       ├── entity/                        # 数据库实体
│       ├── dto/                           # 数据传输对象
│       ├── vo/                            # 视图对象
│       ├── kafka/                         # Kafka 生产者
│       │   └── UserActionProducer.java
│       ├── util/                          # 工具类
│       │   ├── JwtUtil.java
│       │   └── MinioUtil.java
│       ├── filter/                        # JWT 过滤器
│       │   └── JwtAuthenticationFilter.java
│       ├── security/                      # Spring Security 扩展
│       │   └── UserDetailsServiceImpl.java
│       ├── handler/                       # 全局异常处理
│       │   └── GlobalExceptionHandler.java
│       ├── constant/                      # 常量
│       └── task/                          # 定时任务
│           └── TempFileCleanupTask.java
│
├── lakemart-admin/                        # 管理端前端 (Vue 3)
│   ├── Dockerfile
│   ├── nginx.conf
│   ├── package.json
│   ├── vite.config.ts
│   └── src/
│       ├── api/                           # API 接口封装
│       ├── assets/style/                  # 全局样式 (SCSS)
│       ├── components/                    # 公共组件
│       ├── layout/                        # 布局组件
│       ├── router/                        # 路由配置
│       ├── stores/                        # Pinia 状态管理
│       └── views/                         # 页面组件
│           ├── Dashboard.vue              # 仪表盘
│           ├── Products.vue               # 商品管理
│           ├── Orders.vue                 # 订单管理
│           ├── Users.vue                  # 用户管理
│           ├── Categories.vue             # 分类管理
│           ├── Banner.vue                 # 轮播图管理
│           ├── Profile.vue                # 个人中心
│           └── Login.vue                  # 登录页
│
├── lakemart-client/                       # 用户端前端 (Vue 3)
│   ├── Dockerfile
│   ├── nginx.conf
│   ├── package.json
│   ├── vite.config.ts
│   └── src/
│       ├── api/
│       ├── components/
│       ├── router/
│       ├── stores/
│       └── views/
│           ├── HomeView.vue               # 商品列表 + 轮播图
│           ├── ProductDetail.vue          # 商品详情
│           ├── Cart.vue                   # 购物车
│           ├── Address.vue                # 地址管理
│           ├── OrderCreate.vue            # 下单
│           ├── Orders.vue                 # 订单列表
│           ├── OrderDetail.vue            # 订单详情
│           ├── Profile.vue                # 个人中心
│           ├── PointsLogs.vue             # 积分明细
│           ├── Login.vue                  # 登录
│           ├── Register.vue               # 注册
│           └── ForgotPassword.vue         # 忘记密码
│
└── lakemart-spark/                        # 大数据 Spark 模块（独立 Maven 模块）
    ├── pom.xml
    └── src/main/java/org/lzx/lakemart/spark/
        ├── streaming/
        │   └── HotProductsStreaming.java   # Spark Streaming 实时热门商品
        ├── batch/
        │   └── (待开发)                    # 离线画像/日报
        └── SimpleConsumer.java             # 测试消费者
```

---

## 四、部署配置（Docker Compose）

### 端口映射

| 服务 | 宿主机端口 | 容器端口 | 说明 |
|------|------------|----------|------|
| MySQL | 3307 | 3306 | 业务数据库 |
| Redis | 6379 | 6379 | 缓存 |
| MinIO | 9000/9001 | 9000/9001 | 对象存储/控制台 |
| Zookeeper | 2181 | 2181 | Kafka 协调 |
| Kafka | 9092 | 9092 | 消息队列 |
| Backend | 8080 | 8080 | Spring Boot 后端 |
| Admin Frontend | 5173 | 80 | 管理端前端 |
| Client Frontend | 5174 | 80 | 用户端前端 |

### 数据持久化卷
- `./mysql-data` → MySQL 数据
- `./redis-data` → Redis 数据
- `./minio-data` → MinIO 数据
- `./lakemart-spark/target/lakemart-spark.jar` → Spark 作业 JAR

### 网络
- 所有服务通过 `lakemart-net` 桥接网络通信，内部使用服务名（如 `mysql`、`kafka`、`redis`）互相访问。

---

## 五、开发环境与运行方式

### 混合开发模式（当前推荐）

| 组件 | 运行方式 | 说明 |
|------|----------|------|
| MySQL, Redis, MinIO, Kafka, Zookeeper | Docker 容器 | 基础设施常驻运行 |
| 后端 (Spring Boot) | IDEA 本地运行 | 代码修改立即生效，便于调试 |
| 管理端/用户端前端 | 本地 `npm run dev` 或 Docker 容器 | 开发时建议本地运行（需 Node 20+） |
| Spark 作业 | IDEA 直接运行 main 方法 | 本地测试连通性 |

### 启动命令

```bash
# 启动所有基础设施服务（不含前后端）
docker-compose up -d mysql redis minio kafka zookeeper

# 启动后端（本地 IDEA）
运行 LakeMartApplication.main()

# 启动前端（本地 Node）
cd lakemart-admin && npm run dev  # http://localhost:5173
cd lakemart-client && npm run dev # http://localhost:5174

# 查看 Kafka 消息
docker exec -it lakemart-kafka kafka-console-consumer --bootstrap-server localhost:9092 --topic user-actions --from-beginning
```

---

## 六、当前功能完成度

### ✅ 已完成（电商核心）

**管理端**
- 用户登录（JWT，角色 `ROLE_ADMIN` / `ROLE_USER`）
- 商品管理（CRUD、上下架、MinIO 图片上传、分类筛选、分页）
- 订单管理（列表、状态筛选、发货/完成/取消）
- 用户管理（列表、禁用/启用、重置密码、调整积分）
- 分类管理（树形结构、增删改查、状态切换）
- 轮播图管理（CRUD、MinIO 图片上传）
- 仪表盘（订单趋势、销售额趋势、热销商品排行）
- 个人中心（修改密码、头像上传、手机号/邮箱/昵称/简介）

**用户端**
- 注册/登录（邮箱验证码）
- 商品浏览（列表、分类筛选、搜索、排序、分页）
- 购物车（添加、修改数量、删除、清空）
- 收货地址管理
- 下单（选择地址、生成订单）
- 订单管理（列表、支付模拟、取消、确认收货）
- 积分明细
- 个人中心（同管理端）

**基础设施**
- JWT + Spring Security 认证
- MinIO 对象存储
- Docker Compose 一键部署
- 全局异常处理
- GitHub 开源仓库

---

### 🚧 进行中 / 待开发（大数据模块）

| 模块 | 状态 | 说明 |
|------|------|------|
| 用户行为日志采集 (Kafka) | ✅ 已完成 | `/api/user/action` 发送消息到 Kafka `user-actions` |
| Spark 消费 Kafka 测试 | ✅ 已完成 | `SimpleConsumer.java` 可打印消息 |
| **Spark Streaming 实时热门商品** | 🔄 待实现 | 滑动窗口统计，写入 Redis ZSET |
| **Spark Batch 离线用户画像** | ⏳ 未开始 | 每日从 MinIO 读 Parquet，生成标签 |
| **Spark Batch 销售日报** | ⏳ 未开始 | 每日聚合订单数据，写入 MySQL |
| **管理端实时热门商品展示** | ⏳ 未开始 | 从 Redis 读取，新增仪表盘卡片 |
| **Iceberg 表格式管理日志** | ⏳ 未开始 | 湖仓一体增强 |

---

## 七、关键技术细节

### 用户行为日志采集链路

**前端埋点调用**（示例，`ProductDetail.vue`）：
```typescript
const trackView = () => {
  const token = localStorage.getItem('token')
  if (!token) return
  axios.post('/api/user/action', {
    actionType: 'view_product',
    productId: product.value.id
  }, { headers: { Authorization: `Bearer ${token}` } })
}
```

**后端接口** (`UserController.collectAction`)：
- 接收 JSON，补充 `userId`, `timestamp`, `ip`, `userAgent`
- 通过 `UserActionProducer` 发送到 Kafka topic `user-actions`

**Kafka 配置**（`application.yml`）：
```yaml
spring:
  kafka:
    bootstrap-servers: ${KAFKA_BOOTSTRAP_SERVERS:kafka:9092}
    producer:
      key-serializer: org.apache.kafka.common.serialization.StringSerializer
      value-serializer: org.springframework.kafka.support.serializer.JsonSerializer
```

### Spark 作业关键配置

- **本地运行**：`master("local[*]")`
- **Kafka 连接**：`bootstrap.servers = localhost:9092`
- **窗口大小**：5 分钟
- **输出目标**：Redis ZSET (`hot:products`)

---

## 八、常用调试命令

```bash
# 查看所有容器状态
docker ps --format "table {{.Names}}\t{{.Status}}"

# 查看 Kafka 消息
docker exec -it lakemart-kafka kafka-console-consumer --bootstrap-server localhost:9092 --topic user-actions --from-beginning

# 查看 Redis 热门商品数据
docker exec -it lakemart-redis redis-cli ZREVRANGE hot:products 0 9 WITHSCORES

# 查看后端日志
docker logs lakemart-backend -f

# 重启 Kafka（如需）
docker-compose restart kafka
```

---

## 九、项目状态与下一步计划

### 当前状态
- 电商核心功能稳定，可正常运行。
- 日志采集链路打通，Kafka 中已有消息。
- Spark 消费者测试通过，可打印消息。
- 待实现：实时聚合写入 Redis、离线画像、前端展示。

### 下一步计划（按优先级）
1. **实现 Spark Streaming 实时统计**：消费 Kafka，按 5 分钟窗口聚合点击量，写入 Redis。
2. **管理端实时热门商品展示**：新增接口 `/api/admin/statistics/hot-products`，前端仪表盘增加卡片。
3. **实现离线用户画像**：每天从 MinIO 读取 Parquet，计算标签，存入 MySQL。
4. **实现离线日报**：每天统计 GMV、订单数等，存入 MySQL。
5. **湖仓一体增强**（可选）：使用 Iceberg 管理 MinIO 上的 Parquet 文件。

---

## 十、注意事项 & 约束

- **JDK 版本**：统一使用 JDK 21（后端 + Spark）。
- **Node 版本**：前端需要 Node 20.19+（低于此版本会报 `CustomEvent` 错误）。
- **Docker 网络**：所有服务必须加入 `lakemart-net`，才能通过服务名通信。
- **Kafka 连接**：
    - 本地运行 Spark/后端时，使用 `localhost:9092`
    - Docker 容器内访问时，使用 `kafka:9092`
- **环境变量**：敏感信息通过环境变量注入（已配置占位符）。
- **已有账号**：管理员账号 `admin2@example.com` / `12345678`（需自行注册或手动插入）。

---