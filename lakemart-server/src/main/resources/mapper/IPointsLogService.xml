<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="org.lzx.lakemart.mapper.OrderItemMapper">
    <insert id="batchInsert">
        INSERT INTO tb_order_item (order_id, product_id, product_name, price, quantity, create_time)
        VALUES
        <foreach collection="list" item="item" separator=",">
            (#{item.orderId}, #{item.productId}, #{item.productName}, #{item.price}, #{item.quantity}, NOW())
        </foreach>
    </insert>

    <select id="selectByOrderId" resultType="org.lzx.lakemart.model.entity.OrderItem">
        SELECT * FROM tb_order_item WHERE order_id = #{orderId}
    </select>
</mapper>
