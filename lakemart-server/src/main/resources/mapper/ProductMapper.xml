<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="org.lzx.lakemart.mapper.ProductMapper">
    <update id="decreaseStock">
        UPDATE tb_product SET stock = stock - #{quantity} WHERE id = #{id} AND stock >= #{quantity}
    </update>
    <update id="increaseStock">
        UPDATE tb_product SET stock = stock + #{quantity} WHERE id = #{id}
    </update>
</mapper>
