# LakeMart 湖仓一体电商数据平台 — 项目快照（当前状态）

> 你可以将此文档作为 Prompt 发送给任何 AI 助手，让其快速理解项目的完整上下文，以便继续开发、调试或重构。以下内容请**完整复制**，在需要时一次性发送。

---

## 项目概述

LakeMart 是一个**湖仓一体架构**的 B2C 电商数据平台，融合了**完整的电商业务闭环**与**实时/离线大数据分析能力**。项目定位为个人技术实践项目，已具备商品管理、订单交易、用户行为日志采集、实时热门商品统计、离线用户画像生成、个性化推荐系统等完整功能。项目采用前后端分离架构，后端基于 Spring Boot 3，前端使用 Vue 3，大数据部分基于 Spark 和 Kafka，所有服务均通过 Docker Compose 一键部署。

**GitHub 地址**：https://github.com/sunkuizhi/LakeMart

---

## 一、技术栈总览

### 后端（Spring Boot 3）
| 组件 | 版本 | 用途 |
|------|------|------|
| Spring Boot | 3.5.13 | 主框架 |
| JDK | 21 | 后端运行环境 |
| MyBatis-Plus | 3.5.13 | ORM 框架 |
| MySQL | 8.0 | 业务数据库（容器） |
| Redis | 7 | 缓存 + 实时热门商品存储 + 推荐缓存（容器） |
| MinIO | 最新 | 对象存储（图片、日志、Iceberg 数据湖）（容器） |
| Kafka | 3.7.0 | 消息队列（容器） |
| Spark | 3.5.0 | 大数据计算引擎（JDK 17） |
| Iceberg | 1.6.1 | 数据湖表格式（存储于 MinIO） |
| JWT | 0.12.6 | 身份认证 |
| Spring Security | 6.5.9 | 权限控制 |
| Spring Mail | — | 邮件发送（验证码） |
| Hutool | 5.8.32 | 工具库 |
| Lombok | — | 简化代码 |

### 前端（Vue 3）
| 组件 | 版本 | 用途 |
|------|------|------|
| Vue | 3.5 | 前端框架 |
| Vite | 8.0.10 | 构建工具 |
| Element Plus | 2.13.7 | UI 组件库 |
| ECharts | 6.0.0 | 数据图表 |
| Axios | — | HTTP 客户端 |
| Pinia | — | 状态管理 |
| TypeScript | — | 类型安全 |

### 部署与基础设施
| 组件 | 说明 |
|------|------|
| Docker / Docker Compose | 容器化部署，一键启动全部服务 |
| 数据卷 | mysql-data, redis-data, minio-data（持久化） |
| 网络 | 自定义桥接网络 `lakemart-net` |

---

## 二、项目架构

### 整体架构图（概念）

```
┌─────────────────────────────────────────────────────────────┐
│                      客户端层                               │
│  ┌─────────────┐    ┌─────────────────────────────────────┐ │
│  │ 用户端 (H5)  │    │         管理端 (后台)               │ │
│  │ Vue 3 + EP  │    │      Vue 3 + EP + ECharts          │ │
│  └─────────────┘    └─────────────────────────────────────┘ │
└──────────────────────────┬──────────────────────────────────┘
                           │ HTTPS / HTTP
┌──────────────────────────▼──────────────────────────────────┐
│                    业务层 (Spring Boot)                     │
│  ┌─────────┐ ┌─────────┐ ┌─────────┐ ┌───────────────────┐ │
│  │ 用户认证 │ │ 商品管理 │ │ 订单管理 │ │ 购物车/地址/积分  │ │
│  └─────────┘ └─────────┘ └─────────┘ └───────────────────┘ │
│  ┌─────────┐ ┌─────────┐ ┌─────────┐ ┌───────────────────┐ │
│  │分类管理  │ │轮播图管理│ │仪表盘   │ │ 个人中心          │ │
│  └─────────┘ └─────────┘ └─────────┘ └───────────────────┘ │
│  ┌─────────────────────────────────────────────────────────┐ │
│  │         日志采集 API (/api/behavior/track)              │ │
│  └─────────────────────────────────────────────────────────┘ │
│  ┌─────────────────────────────────────────────────────────┐ │
│  │         推荐系统 API (/api/user/recommend)             │ │
│  └─────────────────────────────────────────────────────────┘ │
└──────────────────────────┬──────────────────────────────────┘
                           │
┌──────────────────────────▼──────────────────────────────────┐
│                      数据层                                 │
│  ┌─────────┐ ┌─────────┐ ┌─────────┐ ┌───────────────────┐ │
│  │  MySQL  │ │  Redis  │ │  MinIO  │ │     Kafka         │ │
│  │(业务数据)│ │(缓存/热榜)│ │(图片/日志)│ │   (消息队列)      │ │
│  └─────────┘ └─────────┘ └─────────┘ └───────────────────┘ │
└──────────────────────────┬──────────────────────────────────┘
                           │
┌──────────────────────────▼──────────────────────────────────┐
│                   大数据计算层                              │
│  ┌──────────────────────┐    ┌────────────────────────────┐ │
│  │   Spark Streaming    │    │      Spark Batch           │ │
│  │  (实时数据入湖)      │    │  (热销/趋势/分布/用户画像)  │ │
│  └──────────────────────┘    └────────────────────────────┘ │
└─────────────────────────────────────────────────────────────┘
```

### 数据流路径
1. **实时流**：前端埋点 → `/api/behavior/track` → Kafka (`user-behaviors`) → Spark Streaming → Iceberg (MinIO) + Redis (热榜) + MySQL (行为日志)
2. **离线流**：Iceberg → Spark Batch → MySQL (`hot_products`, `behavior_trend`, `action_distribution`, `user_profile`)
3. **推荐流**：用户行为 → 实时/离线计算 → Redis 缓存 → `/api/user/recommend` → 前端展示

---

## 三、项目目录结构（当前状态）

```
LakeMart/                                  # 项目根目录（Maven 父工程）
├── docker-compose.yml                     # Docker Compose 编排文件
├── .gitignore
├── README.md
├── pom.xml                                # 父 POM
│
├── lakemart-server/                       # 后端 Spring Boot 模块
│   ├── Dockerfile
│   ├── pom.xml
│   ├── sql/                               # 建表脚本
│   │   └── lakemart.sql
│   └── src/main/java/org/lzx/lakemart/
│       ├── LakeMartApplication.java       # 主启动类
│       ├── config/                        # 配置类
│       ├── controller/
│       │   ├── admin/                     # 管理端 Controller
│       │   │   ├── AdminBannerController.java
│       │   │   ├── AdminCategoryController.java
│       │   │   ├── AdminOrderController.java
│       │   │   ├── AdminProductController.java
│       │   │   ├── AdminStatisticsController.java
│       │   │   └── AdminUserController.java
│       │   └── client/                    # 用户端 Controller
│       │       ├── AddressController.java
│       │       ├── BannerController.java
│       │       ├── BehaviorController.java
│       │       ├── CartController.java
│       │       ├── CategoryController.java
│       │       ├── OrderController.java
│       │       ├── ProductController.java
│       │       └── UserController.java
│       ├── service/
│       │   ├── admin/                     # 管理端 Service 接口
│       │   ├── client/                    # 用户端 Service 接口
│       │   ├── common/                    # 公共 Service 接口
│       │   └── impl/
│       │       ├── admin/                 # 管理端 Service 实现
│       │       ├── client/                # 用户端 Service 实现
│       │       └── common/                # 公共 Service 实现
│       ├── mapper/                        # MyBatis-Plus Mapper
│       ├── model/                         # entity, dto, vo
│       │   └── vo/
│       │       ├── RecommendProductVO.java  # 推荐商品 VO（含推荐理由）
│       ├── kafka/                         # Kafka 生产者
│       ├── util/                          # 工具类
│       ├── filter/                        # JWT 过滤器
│       ├── security/                      # Spring Security 扩展
│       ├── handler/                       # 全局异常处理
│       ├── constant/                      # 常量
│       └── task/                          # 定时任务
│           ├── DataSimulator.java         # 数据模拟器（可关闭）
│           ├── SparkBatchScheduler.java   # Spark 批处理调度
│           └── TempFileCleanupTask.java   # 临时文件清理
│
├── lakemart-admin/                        # 管理端前端 (Vue 3)
│   ├── Dockerfile
│   ├── nginx.conf
│   ├── package.json
│   ├── vite.config.ts
│   └── src/
│       ├── api/                           # API 接口封装
│       ├── assets/style/                  # 全局样式
│       ├── components/                    # 公共组件
│       ├── layout/                        # 布局组件
│       ├── router/                        # 路由配置
│       ├── stores/                        # Pinia 状态管理
│       └── views/
│           ├── dashboard/
│           ├── product/
│           ├── order/
│           ├── user/
│           │   ├── Users.vue
│           │   └── UserProfile.vue        # 用户画像页面
│           ├── category/
│           ├── banner/
│           ├── forecast/
│           ├── auth/
│           └── profile/
│
├── lakemart-client/                       # 用户端前端 (Vue 3)
│   ├── Dockerfile
│   ├── nginx.conf
│   ├── package.json
│   ├── vite.config.ts
│   └── src/
│       ├── api/
│       ├── components/
│       ├── router/
│       ├── stores/
│       └── views/
│           ├── HomeView.vue               # 首页（含推荐商品区块）
│           ├── ProductDetail.vue
│           ├── Cart.vue
│           ├── Address.vue
│           ├── OrderCreate.vue
│           ├── Orders.vue
│           ├── OrderDetail.vue
│           ├── Profile.vue
│           ├── PointsLogs.vue
│           ├── Login.vue
│           ├── Register.vue
│           └── ForgotPassword.vue
│
└── lakemart-spark/                        # 大数据 Spark 模块
    ├── pom.xml
    └── src/main/java/org/lzx/lakemart/spark/
        ├── streaming/
        │   └── RealtimeBehaviorStreaming.scala   # 实时数据入湖
        ├── batch/
        │   ├── HotProductsBatch.scala            # 热销商品统计
        │   ├── BehaviorTrendBatch.scala          # 行为趋势统计
        │   ├── ActionDistributionBatch.scala     # 行为分布统计
        │   └── UserProfileBatch.scala            # 用户画像计算
        └── test/
            └── CheckIcebergData.scala            # Iceberg 数据查看工具
```

---

## 四、核心功能完成度

### ✅ 已完成功能

#### 电商业务（100% 完成）
- **用户端**：注册登录、商品浏览/搜索/分类筛选、购物车、下单、订单管理、收货地址管理、积分明细、个人中心
- **管理端**：商品管理（CRUD、上下架）、订单管理、用户管理、分类管理、轮播图管理、仪表盘（订单趋势、销售额趋势、热销商品排行）

#### 用户行为采集（100% 完成）
- 前端埋点 `POST /api/behavior/track`
- 行为数据写入 Kafka `user-behaviors` 主题
- 行为数据同步写入 MySQL `user_behavior_log` 表

#### 实时数据入湖（100% 完成）
- Spark Streaming 消费 Kafka，解析 JSON
- 写入 Iceberg 表 `lake.default.user_behaviors`（MinIO 存储）
- 写入 MySQL `user_behavior_log`（实时监控展示）

#### 离线批处理（100% 完成）
- `HotProductsBatch`：热销商品 → `hot_products`
- `BehaviorTrendBatch`：行为趋势 → `behavior_trend`
- `ActionDistributionBatch`：行为分布 → `action_distribution`
- `UserProfileBatch`：用户画像 → `user_profile`
- 通过 `SparkBatchScheduler` 定时调度（每 5/10 分钟）

#### 用户画像系统（100% 完成）
- 用户画像 Spark 批处理计算
- 管理端用户画像可视化页面
- 生命周期阶段、用户等级、消费能力等标签

#### 推荐系统（100% 完成）
- `GET /api/user/recommend` 推荐接口（含 Redis 缓存）
- 基于用户画像偏好品类推荐
- 基于相似商品推荐（协同过滤）
- 热销商品兜底
- 推荐理由展示

#### 项目结构重构（100% 完成）
- Controller 层按 `admin/client` 拆分
- Service 层按 `admin/client/common` 拆分
- 删除冗余空 Controller 和 Service

---

### 🚧 待开发/优化方向

| 方向 | 优先级 | 说明 |
|------|--------|------|
| 推荐算法优化 | 中 | 引入实时行为权重、时间衰减 |
| 推荐埋点反馈 | 中 | 记录用户对推荐商品的点击/购买 |
| 部署上线 | 高 | 打包 Docker 镜像，部署到云服务器 |
| 实时告警系统 | 低 | 异常行为监控 + WebSocket 推送 |
| Iceberg 小文件合并 | 低 | 优化数据湖存储性能 |

---

## 五、环境与配置

### Docker 服务端口
| 服务 | 宿主机端口 | 容器端口 |
|------|------------|----------|
| MySQL | 3307 | 3306 |
| Redis | 6379 | 6379 |
| MinIO | 9000/9001 | 9000/9001 |
| Zookeeper | 2181 | 2181 |
| Kafka | 9092 | 9092 |
| Backend | 8080 | 8080 |
| Admin Frontend | 5173 | 80 |
| Client Frontend | 5174 | 80 |

### JDK 版本说明
| 模块 | JDK 版本 | 说明 |
|------|----------|------|
| lakemart-server | JDK 21 | 后端业务 |
| lakemart-spark | JDK 17 | Spark 作业（必须使用 JDK 17） |

### Spark 作业 VM Options
```
--add-opens java.base/sun.nio.ch=ALL-UNNAMED
--add-opens java.base/java.nio=ALL-UNNAMED
--add-opens java.base/java.lang=ALL-UNNAMED
--add-opens java.base/jdk.internal.ref=ALL-UNNAMED
-Dio.netty.tryReflectionSetAccessible=true
-Dspark.ui.enabled=false
```

---

## 六、常见问题与解决

| 问题 | 解决方案 |
|------|----------|
| Spark 作业执行失败 | 检查 JDK 版本是否为 17 |
| Kafka 连接超时 | 启动 Kafka 容器：`docker-compose up -d kafka zookeeper` |
| 推荐接口无数据 | 确保 `hot_products` 表有数据，或运行 `UserProfileBatch` |
| Redis 缓存问题 | `docker exec -it lakemart-redis redis-cli FLUSHDB` |
| `@RequestParam` 参数绑定失败 | 确保所有 `@RequestParam` 显式指定 `name` 属性 |

---

## 七、常用调试命令

```bash
# 启动所有基础设施
docker-compose up -d

# 查看所有容器状态
docker ps

# 查看 Kafka 消息
docker exec -it lakemart-kafka /opt/kafka/bin/kafka-console-consumer.sh --bootstrap-server localhost:9092 --topic user-behaviors --from-beginning

# 查看 Redis 热榜数据
docker exec -it lakemart-redis redis-cli ZREVRANGE hot:products 0 9 WITHSCORES

# 查看 Redis 推荐缓存
docker exec -it lakemart-redis redis-cli KEYS "recommend:*"

# 清空 Redis
docker exec -it lakemart-redis redis-cli FLUSHDB

# 查看 MinIO Iceberg 数据
# 访问 http://localhost:9001，账号 minioadmin/minioadmin
```

---

## 八、项目状态总结

| 模块 | 状态 |
|------|------|
| 电商业务（用户端 + 管理端） | ✅ 稳定运行 |
| 用户行为采集（埋点 + Kafka） | ✅ 正常 |
| 实时数据入湖（Spark Streaming） | ✅ 正常 |
| 离线批处理（热销/趋势/分布） | ✅ 正常 |
| 用户画像（Spark Batch） | ✅ 正常 |
| 推荐系统（含 Redis 缓存） | ✅ 正常 |
| 项目结构重构 | ✅ 已完成 |
| 部署上线 | ⏳ 待完成 |

---

## 九、注意事项

- **JDK 版本**：后端用 JDK 21，Spark 模块必须用 JDK 17
- **Kafka 连接**：本地用 `localhost:9092`，Docker 内用 `kafka:9092`
- **DataSimulator 模拟器**：已通过删除 `spring.profiles.active` 关闭，如需重新启用可恢复
- **Redis 缓存**：推荐结果缓存 1 小时，修改推荐逻辑后需手动清空缓存
- **所有 Controller 和 Service 已按 `admin/client/common` 分包**

---

此快照可用于后续对话中快速让 AI 理解项目全貌。如需进一步细化某一部分，可在此基础上补充。
此快照可用于后续对话中快速让 AI 理解项目全貌。如需进一步细化某一部分，可在此基础上补充。